export function LoadingState() {
  return (
    <div className="app-state-card">
      <p className="section-kicker">Bootstrapping</p>
      <h2>Loading demo case</h2>
      <p>Fetching the shared case record and backend-backed workflow state.</p>
    </div>
  );
}
